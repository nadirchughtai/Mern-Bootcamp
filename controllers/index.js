//controllers are used to interaction with external server.
const usersControllers = require('./userscontroller');
const adminControllers = require('./admincontroller');
const clientControllers = require('./clientcontroller');

module.exports = {
  usersControllers,
  adminControllers,
  clientControllers,
};
